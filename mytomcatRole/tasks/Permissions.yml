---
- name: grant tomcat ownership on /opt/tomcat/ folder
  command: sudo chown -R tomcat:tomcat /opt/tomcat/

- name: change to execute permission on bin folder -R u+x  
  command: sudo chmod -R u+x /opt/tomcat/bin 

  